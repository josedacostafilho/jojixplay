---
status: Active
last_verified: 2026-08-17
scope: Current repository state, risks, and immediate decisions
---

# Project status

## Snapshot

| Field | Current truth |
| --- | --- |
| Lifecycle | Greenfield / three-game paired and all-in-one prototype implemented / pre-release |
| Product purpose | Free phone-as-body-controller experiences through paired television or all-in-one phone play; current proof is a live procedural body avatar plus Draw, Bubbles, and Racing |
| Application code | Strict three-mode shell, phone-to-television vertical slice, direct local-play topology, and three shared games implemented |
| Runtime architecture | Implemented under ADR-0002 through ADR-0018 |
| Language and framework | TypeScript and Preact |
| Package/build tooling | Node 24.19.0, npm 11.17.0, and Vite |
| Test tooling | Vitest, Testing Library, and Playwright |
| CI/CD | Full validation on pull requests and `main`; only validated `main` artifacts deploy to GitHub Pages |
| Deployment target | Validated `main` artifacts publish to `https://josedacostafilho.github.io/jojixplay/` through GitHub Pages |
| Persistence model | None by decision |
| Version control | Public GitHub repository on `main` with `origin` configured |
| Compatibility commitment | None; backwards compatibility is forbidden by default |

This snapshot describes observed repository state, not a proposed architecture.

## Current capabilities

- The strict application-mode shell accepts only `mode=tv`, `mode=phone`, or `mode=local`; the removed `role` route fails closed. Mode-specific blocking capability checks, trusted TV-mode/fullscreen entry, television QR/manual-key pairing, paired phone controller, and **Play on this phone** are implemented.
- Local play starts camera/MediaPipe only after **Start local play**, uses the shared camera lifecycle and direct validated in-memory packets, mounts the same mirrored `BodyPlayfield` and games, and creates no key, QR, Nostr/WebSocket, WebRTC, sender, or loopback transport. Its internal capture video is visually hidden and accessibility-hidden; no raw camera preview is rendered. A compact top safety bar owns status and Stop without covering the playfield. Fullscreen and Screen Wake Lock are best-effort, and stop/unmount releases camera, worker, pending requests, games, wake lock, and owned fullscreen.
- A fresh 100-bit pairing key feeds the same domain-separated room/password derivation whether scanned or typed; the former room/secret fragment is rejected.
- The phone requests its user-facing camera only after a user action, loads the vendored MediaPipe Lite model in a module worker, and previews locally detected procedural body avatars. It accepts portrait or landscape, uses validated Screen Orientation plus actual bitmap dimensions to pass MediaPipe one clockwise quarter-turn, maps unrotated results into an upright canonical frame, and increments a frame epoch after a stable basis change. MediaPipe tracking resets at that boundary without restarting camera capture. The camera still requests an ideal/maximum 30 FPS; eligible callbacks run serial inference without a lower elapsed-time gate, while busy or unsettled-orientation frames are dropped. Each pairing session defaults to one-pose inference.
- A collapsed phone-local diagnostics panel reports current screen/source/rotation/canonical/epoch state plus bounded two-second camera, inference, processing-age, and one-player coarse-hand-spread aggregates at most twice per second. It neither transmits nor persists diagnostics or coordinates.
- The Trystero room authenticates exactly one opposite-role peer and carries latest-only, strictly validated canonical pose packets plus strict acknowledged player-limit and camera-layout commands over WebRTC DataChannels. Protocol `jojixplay-skeleton/3` and the `{ width, height, layout, epoch }` frame are hard-cut contracts.
- The television ignores malformed and non-increasing packets and clears stale or disconnected output; local play withholds its last packet after the same one-second freshness bound. `BodyPlayfield` renders up to two identity-independent faceless body avatars through one shared mirrored Canvas projection. One-pose avatar copies use bounded display-only adaptive landmark/segment stabilization and near-side hysteresis; multi-pose avatars use current-packet geometry with no tracking or history.
- A single person claims a temporary controller with one raised hand; in a multiperson frame, one person must raise both. Main Menu uses a frozen semantic row above the controlling head in portrait and a compact left column in landscape. Games, Draw, actionable Bubbles phases, and actionable Racing phases use a compact left column in either supported layout. Every action surface requires the coarse hand to leave its targets once before deliberate dwell activation arms, and every camera epoch releases temporal control state. Bubbles and Racing actionable message panels stay near the top and below the button layer so informational chrome cannot cover a reachable control.
- Main Menu toggles the background, requests apply-before-display one-/two-player inference, or opens Games. Paired display uses exact acknowledgement; local play waits for the same camera-controller operation directly. Games exposes Draw, Bubbles, Racing, and Return. Draw exposes Pencil/Eraser, Color, a longer-dwell Clear, and Exit. Bubbles exposes Start/Exit while Ready, suspends all action activation through its countdown and round, then exposes Play Again/Exit. Racing exposes Start/Exit while Ready, no actions during calibration/driving, Resume/Recenter/Restart/Exit while Paused, and Play Again/Exit after a finish.
- Draw owns an ephemeral normalized canonical-camera artwork model, an exact white projected-camera board, one selected main-hand Pencil/Eraser cursor, immediate engagement at `≤ 0.75 ×` shoulder span, release at `≥ 1.25 ×` shoulder span, speed-aware path smoothing, fail-closed path breaks, and a 24%-opacity live avatar overlay. It supports either entry layout, cancels grip/path during an active mismatch, retains art while returning to the captured layout, and clears art before deliberate re-entry under the other layout. Controls and Draw continue to use unsmoothed consumer-specific signals rather than the avatar's private display copy.
- Bubbles owns an ephemeral normalized projected-camera arena, six one-player or eight two-player procedurally rendered targets, a three-second countdown, an exact 60-second monotonic round, radius-safe smooth drift/reflection, both-hand point and fresh swept collision, bounded pop/respawn effects, corner HUD counters/timer, a 16%-opacity avatar overlay, and final score/winner/tie results. One-player accepts either layout; two-player is gated on an acknowledged landscape frame. An active mismatch freezes its complete timing/simulation state, while ordinary pose loss does not. Two-player points belong to current mirrored left/right screen slots rather than pose-array positions or stable identities.
- Racing owns an ephemeral deterministic segmented course, pure pseudo-3D projection with an explicit near-road sample and below-horizon terrain base, three-second fresh-torso natural-stance calibration, discrete full steering at `8°` lean with a `3°` release threshold, `80 ms` steering response, `150 ms` observation grace, automatic throttle, curve drift, off-road drag, and a bounded `60 Hz` fixed-step simulation. One-player accepts either layout and reports active elapsed time; two-player requires landscape and uses left/right split screen with bounded local torso leases. Hands affect neither Racing readiness nor steering commands; a separate one-second two-hands-overhead hold pauses once. The avatar is absent; each car view has a restrained torso-lean/tracking gauge. One lazy `phaser@4.2.1` runtime is forced to Canvas and fully destroyed on exit, with no WebGL or renderer fallback.
- Unit/component tests and production-browser smoke tests cover the deterministic boundaries, including strict mode routing, obsolete-route rejection, mode-specific capabilities, shared camera lifecycle, direct local packets, apply-before-display reconfiguration, freshness, hidden capture presentation, optional immersive cleanup, zero local peer creation, and fake-camera MediaPipe initialization in both phone modes.
- The repository is cut over to Node 24, jsdom 30, immutable GitHub Action SHAs, grouped Dependabot updates, pull-request validation, and least-privilege publication jobs. GitHub reports Dependabot vulnerability alerts and automatic security-update pull requests enabled.
- GitHub Actions validates and publishes the static artifact to the live GitHub Pages project site.

## Immediate work

Run the one-player Lite baseline in [Pose quality](../engineering/pose-quality.md), then validate both complete topologies on the project owner's real hardware. Paired acceptance covers portrait, both landscape directions, reported orientation/source metadata, preview alignment, horizontal TV mirroring, stable epoch transitions, achieved camera-paced inference cadence, avatar rest stability and fast-motion lag, menus, game gates/recovery, controls, all three games, cleanup, and thermals. Local acceptance additionally covers no-preview framing, the safety bar, direct player/layout changes, best-effort fullscreen/wake behavior, combined inference/render cadence and thermals, standalone play, and operating-system or wired mirroring latency/quality. This evidence must drive any orientation, threshold, model, collision, steering, course, or presentation replacement; it does not justify alternate runtime paths.

## Pre-release exit criteria

- A real phone and television complete TV-mode/fullscreen entry, QR and manual-key pairing, portrait/landscape camera startup, canonical pose delivery, layout requests, Main Menu/Games/Draw/Bubbles/Racing navigation, active-game rotation recovery, stale/disconnect behavior, and cleanup.
- A real phone completes **Play on this phone** setup, hidden capture, direct fresh-packet play, player/layout changes, every shared game, optional fullscreen/wake behavior, explicit stop, and cleanup both standalone and through the owner's external mirroring path.
- Default one-person detection, acknowledged one-/two-player switching, achieved inference cadence, processing-age p95, both landscape rotations, horizontal mirrored avatar presentation, avatar rest stability/fast-motion lag, portrait overhead framing, compact game-menu reach, stationary coarse-hand spread, temporary controller claiming, transition re-arming, unobscured actionable panels, Draw grip/path continuity, Bubbles gating/timing/hit tolerance/edge containment/side attribution, Racing natural-stance calibration/lean hysteresis/road coverage/pause/split-screen behavior, per-renderer stability, thermal behavior, and perceived latency are recorded from the target hardware.

## Verification evidence

On 2026-08-17, `npm run validate` passed under Node 24.19.0/npm 11.17.0 after the all-in-one phone hard cut: exact toolchain verification, formatting, linting, 175 unit/component tests, vendored-model integrity, two production builds, eight Chromium end-to-end journeys, and the high-severity dependency audit all passed; npm reported zero vulnerabilities. The browser suite proved the strict three-mode landing and removed-route boundary, both paired-phone entry paths, local play's real first MediaPipe packet with fake camera input, absent camera preview and peer APIs, best-effort fullscreen behavior, camera/wake cleanup, unobscured actionable game panels, and lazy forced-Canvas Racing startup/teardown and near-road coverage. The separate Racing chunk measured `1,384.64 kB` minified / `361.43 kB` gzip and retains Vite's visible large-chunk advisory pending target-device measurement.

## Known risks

| Risk | Impact | Required response |
| --- | --- | --- |
| Smart-TV browser capabilities vary | Fullscreen, pairing, or rendering may fail on the target television | Keep fullscreen best-effort, enforce blocking capability checks for required APIs, and test the owner's hardware |
| Public Nostr relay availability is external | A room may not discover its peer | Surface a terminal pairing error; replace the architecture only through a hard cutover if evidence requires it |
| No TURN service exists | Isolated Wi-Fi clients cannot connect | Treat direct connection failure as unsupported for this prototype |
| Real camera and model behavior is hardware-dependent | Fake media proves initialization, not accuracy, thermals, or sustained latency | Run the documented real-device acceptance pass |
| Camera-paced inference can increase sustained phone load | Power use, heat, and two-player throttling may rise even though work cannot backlog | Record achieved cadence and thermals on target phones before changing the explicit 30 FPS ceiling |
| Pose accuracy, jitter, and model cost are target-device dependent | A model or filter that looks smoother may reduce cadence or add control latency | Record the one-player Lite baseline with local diagnostics; evaluate Full only through the isolated hard-cutover protocol in [Pose quality](../engineering/pose-quality.md) |
| Draw grip ratios are initial body-relative defaults | Hands may be awkward to bring close enough, release unintentionally, or be difficult to spread far enough | Measure close activation, wide release, and continuity on the target setup before replacing either side of the hysteresis contract |
| Pose-control geometry and timing are human factors | The Main Menu overhead row or compact Games/Draw/Bubbles/Racing columns may be tiring to reach, the coarse hand may disappear, or button dwell may misfire | Measure the accepted layouts on real people and replace them only from recorded evidence |
| Draw state grows with retained path segments | Very long sessions or resize replays may tax a television browser | Measure long-session behavior and choose an explicit bound only from evidence |
| Bubbles collision defaults are unmeasured on real bodies | Coarse-hand jitter may cause near misses or unintended pops, especially on small bubbles | Measure either-hand point/sweep hits on the target setup before changing radii or sweep freshness |
| Bubbles uses screen sides instead of player identities | Crossing players transfers later points, and a dropout can affect competitive fairness | Keep explicit Left/Right labeling, test two-player behavior, and add no identity tracker without a replacement decision |
| Layered avatar/Bubbles drawing and Phaser Racing may tax a weak television | Body feedback, bubbles, pseudo-3D road drawing, or split-screen Racing may stutter | Measure each renderer plus one-/two-player Racing on the target television; simplify only the affected canonical path from evidence |
| Local play combines inference and rendering on one phone | Heat, battery drain, or contention may reduce pose or game cadence even when the phone outperforms the television | Measure sustained one-/two-player local play and external mirroring on the owner's phone; change only the evidence-backed canonical bottleneck |
| External screen mirroring is outside JojixPlay | Added latency, scaling, disconnection, or platform-specific setup can affect the local-play experience | Treat mirroring as user-managed, document no universal Cast promise, and record the actual selected OS/wired path during acceptance |
| Fullscreen and Screen Wake Lock are optional browser APIs | Browser chrome may remain or the display may dim during local play | Keep both best-effort, preserve explicit status/Stop behavior without them, and test the target browser's lifecycle |
| Procedural avatar proportions and stabilization are unmeasured on real bodies | The body may look distorted, lag during fast motion, or omit anatomy under occlusion | Record rest, fast-motion, partial-landmark, one-/two-person, phone-preview, Draw, and Bubbles behavior before changing the canonical geometry or constants |
| Runtime player-limit replacement is device-dependent | `setOptions()` may pause or fail on a target phone | Keep acknowledgement fail-closed, terminate an uncertain tracking run, and measure switching on real hardware |
| Mobile orientation metadata and camera bitmap behavior vary | A browser may report a delayed/inconsistent angle or already-rotated bitmap, producing a wrong preview or pose basis | Inspect the bounded phone diagnostics in portrait and both landscape directions; keep the strict single normalization path and hard-cut it only from target-device evidence |
| MediaPipe reset on camera-basis changes is device-dependent | `setOptions()` may pause or fail during rotation | Drop unsettled frames, acknowledge only the committed layout, fail closed on reset failure, and measure transition delay on the target phone |
| Nominal aspect ratio does not prove camera field of view | Forcing `4:3` or `16:9` could crop useful coverage without improving vertical framing | Preserve the delivered aspect ratio and compare camera modes only from device evidence |
| Phaser adds a large lazy chunk and target-TV runtime cost | First Racing entry may load slowly or sustained Canvas split-screen may miss frame cadence | Keep Phaser Racing-only, inspect production chunking, and measure startup/cadence/memory on the target television before changing the one renderer path |
| Racing lean thresholds and temporary driver leases are unmeasured | Lean may steer at rest, require an awkward angle, feel laggy, or chatter; overhead pause may false-trigger; large movement/crossing may release a car slot | Record natural calibration, `8°` enter/`3°` release behavior, steering/dropout, pause, and two-player side behavior; change constants or lease rules only from target evidence and never add stable identity implicitly |
| Compatibility habits can introduce permanent clutter | Multiple paths become de facto contracts | Enforce ADR-0001 and delete displaced paths in the same change |

## Status maintenance

Update this file whenever a snapshot fact, active risk, blocking question, or lifecycle stage changes. Move completed durable events to [Milestones](milestones.md), remove resolved transient items, and keep detailed future work in [Backlog](backlog.md).
